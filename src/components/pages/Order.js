import React from 'react';
import PizzaSelection from '../PizzaSelection';

const Order = () => {
	return (
		<div>
			<section className="pizzaSelection">
				<PizzaSelection />
			</section>
		</div>
	);
};

export default Order;
